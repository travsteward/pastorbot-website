# Disable any unwanted redirects and define only the essential ones

# Netlify redirect rules - processed in order

# Proxy API requests to serverless functions
/oauth/*  /.netlify/functions/callback  200

# SPA fallback - must be last
/*  /index.html  200